# GA4・BigQuery設定手順書

この資料は、コードを読まなくても管理と確認ができるようにしたものです。アプリ側の計測は実装済みです。Google AnalyticsやBigQueryへのログイン・連携操作は、サイト管理者がGoogle側で行います。

## 最初に確認すること

- 使用中のGA4測定IDは `G-C34PHRSJN4` です。
- 回答するたびに `quiz_answer` というイベントを送ります。
- ログイン機能や独自の利用者IDは使いません。
- BigQueryでは、GA4が付ける匿名ブラウザID `user_pseudo_id` を利用者単位の分析に使います。
- 氏名、メールアドレス、問題文は送信しません。
- 広告ブロッカーなどでGA4へ送れなくても、クイズは通常どおり動きます。

## アプリ側で実装済みの作業

- 年度順、復習、タイムアタックの全回答を計測
- 「分からない」を `unknown` として計測
- 問題ID、年度、問番号、肢、正誤、選択回答、モード、回答回数、初回判定、復習連続正解数、回答時間、セッション内の問題番号を送信
- ダブルクリックや連打による同じ回答の重複送信を防止
- 回答後は次の問題へ進むまで回答ボタンを無効化
- GA4が読み込めない場合でもクイズを継続
- プライバシー説明ページと各画面からのリンクを追加

## 管理者がGoogle側で行う作業

1. 公開後にGA4のリアルタイムまたはDebugViewで受信確認
2. 必要な場合だけGA4のカスタム定義を作成
3. GA4とBigQueryを連携し、日次エクスポートを開始
4. BigQueryで集計SQLを実行

## 1. `quiz_answer`が届いているか確認する

### 簡単な確認：リアルタイム

1. Google Analyticsを開き、対象プロパティを選びます。
2. 左メニューから「レポート」→「リアルタイム」を開きます。
3. 公開サイトを別画面で開き、クイズに1問回答します。
4. リアルタイム画面のイベント一覧で `quiz_answer` を探します。

通常は短時間で表示されますが、反映に少し時間がかかる場合があります。

### 詳細な確認：DebugView

1. [Google Tag Assistant](https://tagassistant.google.com/)を開きます。
2. 公開サイトのURLを入力して接続します。
3. 接続した画面でクイズに回答します。
4. GA4の「管理」→「データの表示」→「DebugView」を開きます。
5. イベントの流れから `quiz_answer` を選び、パラメーターを確認します。

確認する主な値：

- `question_label`：`R2-1-ア`など
- `answer_result`：`correct`、`wrong`、`unknown`
- `selected_answer`：`circle`、`cross`、`unknown`
- `quiz_mode`：`year`、`review`、`timeattack`
- `attempt_number`：1以上の回答回数
- `is_first_attempt`：初回は1、2回目以降は0
- `response_time_ms`：0以上の回答時間

DebugViewは、Tag Assistantで自分の端末だけをデバッグ対象にする方法を推奨します。本番サイトの全利用者へ`debug_mode`を付ける必要はありません。

## 2. GA4の通常画面で回答データを見る

イベント名だけなら、「レポート」→「エンゲージメント」→「イベント」で `quiz_answer` を確認できます。

パラメーター別に見たい場合は、「管理」→「データの表示」→「カスタム定義」でイベントスコープのカスタムディメンションを作成します。候補は次のとおりです。

- `answer_result`
- `quiz_mode`
- `year`
- `is_first_attempt`

`question_label`は値が2,000種類以上になるため、高カーディナリティ（値の種類が多い項目）です。GA4標準レポートでは「（other）」へまとめられる可能性があるため、問題・肢別の本集計はBigQueryを推奨します。BigQueryへ出力するだけなら、`question_label`をカスタム定義へ登録する必要はありません。

カスタム定義を作成した場合、レポートで利用可能になるまで24～48時間程度かかることがあります。また、作成前の期間へ通常レポート用のカスタム定義が遡って適用されるわけではありません。

### GA4探索で概算の問題別正答率を見る場合

1. `question_label`と`answer_result`をイベントスコープのカスタムディメンションとして登録します。
2. 「探索」→「自由形式」を開きます。
3. 行に`question_label`、列に`answer_result`、値に「イベント数」を置きます。
4. フィルタでイベント名を`quiz_answer`に限定します。
5. 正解数 ÷ 全回答数で概算の正答率を確認します。

問題数が多いため、継続運用では次項のBigQuery集計を使用してください。

## 3. BigQuery連携

1. GA4の「管理」を開きます。
2. 「サービス間のリンク」または「プロダクトのリンク」から「BigQueryのリンク」を開きます。
3. 「リンク」を選び、保存先のGoogle Cloudプロジェクトを指定します。
4. データの場所を選びます。
5. エクスポート方式は「日次」を有効にします。
6. 「ストリーミング」は無効のままにします。
7. 内容を確認して送信します。

このアプリでは即時集計を必要としないため、安定した日次テーブル `events_YYYYMMDD` を使用します。Streamingは追加費用や当日用の不完全なテーブルを考慮する必要があるため使用しません。

重要：BigQuery連携より前のGA4データは、後から日次イベントテーブルとして遡って出力できません。分析を始める場合は、公開後なるべく早めに連携してください。

### BigQuery Sandboxについて

BigQuery Sandboxは支払い設定なしで試せますが、テーブルやパーティションが60日で期限切れになります。長期的に成長率や年度推移を保存する目的では、請求先を設定した通常版BigQueryを使用してください。料金が心配な場合は、予算アラートも設定してください。

## 4. BigQueryで問題別正答率を確認する

集計SQLは [docs/bigquery_quiz_analysis.sql](docs/bigquery_quiz_analysis.sql) にあります。

実行前に、SQL内の次の2か所を自分の環境へ置き換えます。

- `YOUR_PROJECT_ID`：Google CloudのプロジェクトID
- `YOUR_GA4_PROPERTY_ID`：GA4の数字だけのプロパティID

例：プロパティIDが`123456789`なら、データセットは通常`analytics_123456789`です。

収録している集計：

1. 問題・肢別の初回正答率
2. 問題・肢別の全回答正答率
3. 問題別の回答者数と回答回数
4. 匿名ブラウザ単位の週別正答率推移
5. 最初の50回答と直近50回答を比較した成長率
6. 回答者数が少ない問題を除外できる重要度分析用の基礎集計

## 5. データが届かない場合

次の順に確認してください。

1. 公開サイトでテストしているか（HTMLファイルを直接開くのではなく、公開URLを使う）
2. GA4で対象プロパティを間違えていないか
3. 測定IDが`G-C34PHRSJN4`になっているか
4. 広告ブロッカー、トラッキング防止機能、社内ネットワークでGoogle Analyticsが止められていないか
5. ブラウザの開発者向け画面ではなく、まずリアルタイムまたはTag Assistantで確認したか
6. `quiz_answer`ではなく別のイベント名を探していないか
7. カスタム定義の反映待ちではないか（イベント自体の受信とカスタム定義の反映は別です）
8. BigQueryリンクを作成した日より前のデータを探していないか
9. 日次エクスポートの作成を翌日まで待ったか
10. SQL内のプロジェクトIDとGA4プロパティIDを置き換えたか

## 参考：Google公式資料

- [DebugViewでイベントを確認する](https://support.google.com/analytics/answer/7201382?hl=ja)
- [GA4 BigQuery Export](https://support.google.com/analytics/answer/9358801?hl=ja)
- [GA4 BigQuery Exportのスキーマ](https://support.google.com/analytics/answer/7029846?hl=ja)
- [カスタムディメンションと指標について](https://support.google.com/analytics/answer/14240153?hl=ja)
- [BigQuery Sandbox](https://cloud.google.com/bigquery/docs/sandbox?hl=ja)

